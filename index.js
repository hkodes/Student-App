/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import AppWithUserProvider from './App';

AppRegistry.registerComponent(appName, () => AppWithUserProvider);
